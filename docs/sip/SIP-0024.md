# SIP-0024 — Advanced Match Logic: Character Traits & Half-time
| Field   | Value                     |
| Status  | Implemented ✅            |
| Created | 2026-05-16                |
## Summary
Port the legacy character trait logic and the half-time pause mechanism to the high-performance Go worker. This ensures that match results are not just based on raw skills but also on player personality and tactical pacing.
## Technical Details
### 1. Character Traits in Go
- **Grintoso**: +10% bonus to duels if the team is losing.
- **Introverso**: -5% penalty to pass success if playing away.

The Go scheduler advances one tick per loop; half-time pause is handled in `match_state.half_time_ticks`.
- Pause defaults to 15 ticks.
- Pause is configurable via `GM_HALF_TIME_TICKS`.
- Managers keep a tactical window before second half.
### 3. Injury Logic
- Player `condition`.
- Luck.

## Acceptance Criteria

- [x] Go engine applies at least 3 distinct character trait modifiers.
- [x] Injuries can occur and are recorded in `match_event`.
